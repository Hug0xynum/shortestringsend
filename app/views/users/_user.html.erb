<table>
<% if !current_user?(user)%>
  <tr>
  	<td class="user_profile_picture">
  	  <%= gravatar_for user, :size => 30 %>
  	</td>
  	<td class="user_link_profile">
  		<%= link_to user.nom, user, :class => "listing_users"%>
    </td>
    <% if current_user.admin?%>
  	  <td>
  			 <%= button_to "Supprimer", user ,:onclick => "return(confirm('Are you
sure?'))", :method => :delete, :title => "Supprimer #{user.nom}", :class => "remove_user round"%>
        <!--link_to 'Supprimer', user, action: :delete, method: :delete, confirm: "Are you sure?" , :title => "Supprimer #{user.nom}", :class => "remove_user round" -->
      </td>
    <% end %>
  </tr>
<% end %>
</table>