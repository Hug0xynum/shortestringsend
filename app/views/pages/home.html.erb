<% if signed_in? %>
  <table class="front" summary="Pour visiteur identifié">
    <tr>
      <td class="main">
        <h1 class="micropost">Quoi de neuf ?</h1>
        <%= render 'shared/micropost_form' %>
        <%= render 'shared/feed' %>
      </td>
      <td class="sidebar round">
        <%= render 'shared/user_info' %>
        <%= render 'shared/stats' %>
      </td>
    </tr>
  </table>
<% else %>
	<h1>Short String Send</h1>
    <p>
      Bienvenue sur Short String Send ! Le twitter des sans amis !
    </p>
    <%= link_to "S'inscrire !",  signup_path, :class => "signup_button round" %>
<% end %>